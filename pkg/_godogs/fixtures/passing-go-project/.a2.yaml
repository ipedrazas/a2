# Minimal config so a2 check passes (used by Quick pre-push validation scenario)
profile: cli
target: poc
checks:
  disabled:
    - go:coverage
    - go:deps
    - go:logging
    - common:*
language:
  go: {}
files:
  required: []
